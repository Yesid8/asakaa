{
  "buildCommand": "npm run build:core && cd packages/board && npm install && npm run build && cd examples/demo && npm install && npm run build",
  "outputDirectory": "packages/board/examples/demo/dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
